import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
    },
    fotoUsuario: {
        width: 50,
        height: 50,
        margin: 10,
        borderRadius: 50
    },
    negrito: {
        fontWeight: "bold"
    }
});